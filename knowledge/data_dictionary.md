# Tushare 核心指标卡片库 (Indicator Cards)

本文档采用 **指标卡片 (Indicator Card)** 格式组织，旨在确保 RAG 检索时能同时获取 **定义**、**字段** 和 **用法示例**。

---

## 📂 一、 估值与基础指标 (Valuation & Basics)

### [指标卡片] 市盈率 (PE / PE-TTM)
#### 1. 业务定义
市盈率 (Price-to-Earnings Ratio)。
*   **静态市盈率 (PE)**: 基于上一年年报数据。
*   **滚动市盈率 (PE-TTM)**: 基于最近4个季度的财务数据（动态）。**推荐优先使用 TTM。**
#### 2. 字段名 (Key)
*   静态: `pe`
*   动态: `pe_ttm`
#### 3. 代码示例 (Usage) - MUST READ
```python
# 获取市盈率数据
res = get_daily_basic(trade_date='20241220')
# 筛选逻辑需在 Agent 代码中实现

```

### [指标卡片] 市净率 (PB)
#### 1. 业务定义
市净率 (Price-to-Book Ratio)。股价与每股净资产的比率。适合评估周期股、银行股。
#### 2. 字段名 (Key)
`pb`
#### 3. 代码示例 (Usage)
```python
res = get_daily_basic(trade_date='20241220')

```

### [指标卡片] 股息率 (Dividend Yield)
#### 1. 业务定义
股息率。**高频坑点**：数值直接表示百分比（如 3.5 代表 3.5%）。
*   **股息率**: 基于最近一次分红。
*   **滚动股息率 (TTM)**: 基于过去12个月分红。
#### 2. 字段名 (Key)
*   静态: `dv_ratio`
*   滚动: `dv_ttm`
#### 3. 代码示例 (Usage)
```python
# 获取股息率数据
res = get_daily_basic(trade_date='20241220')
# Agent 遍历筛选 dv_ratio > 5 的股票

```

### [指标卡片] 市销率 (PS)
#### 1. 业务定义
市销率 (Price-to-Sales)。适合评估高增长但也亏损的公司。
#### 2. 字段名 (Key)
*   静态: `ps`
*   滚动: `ps_ttm`
#### 3. 代码示例 (Usage)
```python
res = get_daily_basic(trade_date='20241220')

```

### [指标卡片] 市值 (Market Cap)
#### 1. 业务定义
*   **总市值**: 总股本 * 股价。单位：**万元**。
*   **流通市值**: 流通股本 * 股价。单位：**万元**。
#### 2. 字段名 (Key)
*   总市值: `total_mv`
*   流通市值: `circ_mv`
#### 3. 代码示例 (Usage)
```python
res = get_daily_basic(trade_date='20241220')
# data = res['data']...

```

### [指标卡片] 换手率 (Turnover Rate)
#### 1. 业务定义
股票转手买卖的频率。情绪指标。单位：%。
#### 2. 字段名 (Key)
`turnover_rate`
#### 3. 代码示例 (Usage)
```python
res = get_daily_basic(trade_date='20241220')

```

### [指标卡片] 量比 (Volume Ratio)
#### 1. 业务定义
量能指标。开盘后平均每分钟成交量与过去5天平均值的比值。
#### 2. 字段名 (Key)
`volume_ratio`

---

## 📂 二、 市场行情数据 (Market Data)

### [指标卡片] 量价数据 (OHLCV)
#### 1. 业务定义
基础行情数据 (Daily)。
*   **成交量 (Vol)**: 单位是 **手** (100股)。
*   **成交额 (Amount)**: 单位是 **千元**。
#### 2. 字段名 (Key)
*   收盘: `close`
*   开盘: `open`
*   高: `high`
*   低: `low`
*   量: `vol`
*   额: `amount`
*   涨跌幅: `pct_chg` (%)
#### 3. 代码示例 (Usage)
```python
df = pro.daily(ts_code='000001.SZ')
close_price = df['close']
volume = df['vol'] * 100 # 换算成股
amount = df['amount'] * 1000 # 换算成元
```

---

## 📂 三、 财务能力 (Financials)

### [指标卡片] 盈利能力 (ROE/Margins)
#### 1. 业务定义
*   **ROE**: 净资产收益率 (%)
*   **毛利率**: Gross Margin (%)
*   **净利率**: Net Profit Margin (%)
#### 2. 字段名 (Key)
*   ROE: `roe`
*   毛利率: `gross_margin`
*   净利率: `netprofit_margin`
#### 3. 代码示例 (Usage)
```python
# 获取盈利能力数据 (ROE, 毛利率)
res = get_financial_indicator(period='20240930')
# 需使用季报截至日期

```

### [指标卡片] 利润表核心 (Revenue/Profit TTM)
#### 1. 业务定义
*   **总营收**: Total Revenue TTM (滚动, 元)
*   **净利润**: Net Profit TTM (归母滚动, 元)
*   **注意**: 这些字段由市值 (MV) 除以 市销率(PS)/市盈率(PE) 推导得出，提供**过去12个月 (TTM)** 的累计值。
#### 2. 字段名 (Key)
*   营收: `total_revenue_ttm`
*   净利: `net_profit_ttm`
#### 3. 代码示例 (Usage)
```python
# 获取营收和净利润数据 (无需调用 get_income_statement)
res = get_daily_basic(trade_date='20241220')
# data = res['data']
# revenue = item['total_revenue_ttm']
```

---

## 📂 四、 通用标识 (Identity)

### [指标卡片] 股票代码 (TS Code)
#### 1. 定义
Tushare 标准代码格式，如 `000001.SZ`, `600519.SH`。
#### 2. 字段名 (Key)
`ts_code`
#### 3. 注意事项
Agent 请优先使用 `ts_code` 作为主键。但在 Tool 内部已做了 `code` 映射。

---

## 📂 五、 概念与板块 (Concepts & Blocks)

### [指标卡片] 概念列表 (Concept List)
#### 1. 业务定义
获取市场概念或行业列表。
*   **Tushare概念**: `src='ts'` (默认)
*   **同花顺概念**: `src='ths'`
#### 2. 工具 (Tool)
`get_concepts(src='ts')`
#### 3. 代码示例 (Usage)
```python
# 获取所有概念
concepts = get_concepts(src='ts')
# 查找半导体相关概念
semicon_concepts = [c for c in concepts if '半导体' in c['name']]
```

### [指标卡片] 概念成分股 (Concept Stocks)
#### 1. 业务定义
获取属于特定概念/行业的所有股票。
#### 2. 工具 (Tool)
`get_concept_stocks(id='...')`
#### 3. 代码示例 (Usage)
```python
# id 必须是 get_concepts 返回的 code
stocks = get_concept_stocks(id='TS123')
```

---

## 📂 六、 全市场筛选 (Market Screener)

### [指标卡片] 每日全市场行情 (Market Daily)
#### 1. 业务定义
**选股器 (Screener) 核心工具**。一次性获取某日**全市场**所有股票的行情。
*   **用途**: 查找涨停股、跌停股、放量股等。
*   **注意**: 每日数据通常在 **17:00** 后更新。如需盘中数据或最新数据，请优先使用 **上一交易日 (Yesterday)**。
#### 2. 工具 (Tool)
`get_market_daily(trade_date='YYYYMMDD')`
#### 3. 常见业务逻辑 (Business Rules)
*   **涨跌停判断 (Limit Up/Down)**:
    *   **主板 (Main Board)**: 涨跌幅限制 $\pm 10\%$ (`pct_chg > 9.8` 或 `pct_chg < -9.8`)
    *   **创业板/科创板 (ChiNext/STAR)**: 涨跌幅限制 $\pm 20\%$ (`pct_chg > 19.8` 或 `pct_chg < -19.8`, 代码 `300`/`688` 开头)
    *   **ST股**: 涨跌幅限制 $\pm 5\%$
    *   **Agent 思考**: 若需查询“涨停股”，请获取全市场数据后，编写 Python 代码根据上述规则筛选 `pct_chg`。
#### 4. 代码示例 (Usage)
```python
# 获取某日全市场行情
res = get_market_daily(trade_date='20241220')
if res['status'] == 'success':
    df = pd.DataFrame(res['data'])
    # Agent 需自行根据业务规则编写筛选逻辑
```
